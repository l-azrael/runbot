# --coding:utf-8--
# Auto-generated by RunBot maker. DO NOT EDIT manually.
from typing import Optional, Any
from attrs import define, field
{% for model in models %}


@define
class {{ model.name }}:
    {% if model.description %}"""{{ model.description }}"""
    {% endif %}
    {% for f in model.fields %}
    {% set has_meta = f.description or (f.example is not none) or f.enum %}
    {{ f.name }}: {{ f.type_hint }} = field(
        default=None,
        {% if has_meta %}
        metadata={
            {% if f.description %}
            "description": {{ f.description | tojson }},
            {% endif %}
            {% if f.example is not none %}
            "example": {{ f.example | tojson }},
            {% endif %}
            {% if f.enum %}
            "enum": {{ f.enum | tojson }},
            {% endif %}
        },
        {% endif %}
    )
    {% endfor %}
{% endfor %}
