你是一个资深接口自动化测试工程师。请根据以下接口定义，生成完整的 pytest 测试用例文件。

## 接口定义

```python
{api_definition}
```

## 生成要求

1. 测试场景覆盖：
   - 正常流程（至少 2 个正向用例）
   - 边界值（字段长度、数值边界）
   - 异常参数（必填字段缺失、类型错误、枚举值非法）

2. 代码规范：
   - 使用 `@parametrize` 数据驱动，每条数据包含 `desc` 字段说明测试意图
   - 测试类继承 `BaseTestcase`
   - 断言必须包含 `code` 字段校验
   - 文件顶部加 `# Auto-generated by RunBot AI. Review before use.`

3. 输出格式：
   - 只输出 Python 代码，不要任何解释文字
   - 代码可直接运行，import 路径基于项目根目录

## 项目目录结构

测试用例按以下目录结构组织：
```
testcases/<module>/
├── cases/          # 测试用例文件
├── factory/        # 数据工厂文件
└── data/           # 静态数据文件（JSON/YAML）
```

生成的用例文件放在 `testcases/<module>/cases/` 目录下。
数据工厂的 import 路径为 `testcases.<module>.factory.<factory_file>`。

## 参考模板

```python
# Auto-generated by RunBot AI. Review before use.
import pytest
from core.parametrize import parametrize
from core.base_testcase import BaseTestcase
from apis.{module}.apis import {APIClass}
from apis.{module}.models import {RequestModel}


class Test{APIClass}(BaseTestcase):

    @parametrize([
        {{"desc": "正常创建", "{field}": "{example}", ...}},
        {{"desc": "字段为空", "{field}": "", ...}},
    ])
    def test_{method}(self, data):
        body = {RequestModel}(**{{k: v for k, v in data.items() if k != "desc"}})
        res = {APIClass}(request_body=body).send()
        self.assert_eq(res.body.get("code"), 0, data["desc"])
```
