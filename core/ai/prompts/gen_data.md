你是一个资深接口自动化测试工程师。请根据以下接口定义，生成测试数据工厂类。

## 接口定义

```python
{api_definition}
```

## 生成要求

1. 继承 `BaseFactory`，实现 `data()` 类方法
2. 每条数据包含 `desc` 字段说明测试意图
3. 覆盖：正常值、边界值、异常值三类
4. 文件顶部加 `# Auto-generated by RunBot AI. Review before use.`
5. 只输出 Python 代码，不要任何解释文字

## 项目目录结构

数据工厂按以下目录结构组织：
```
testcases/<module>/
├── cases/          # 测试用例文件
├── factory/        # 数据工厂文件
└── data/           # 静态数据文件（JSON/YAML）
```

生成的数据工厂文件放在 `testcases/<module>/factory/` 目录下。

## 参考模板

```python
# Auto-generated by RunBot AI. Review before use.
from testcases.base_data_factory import BaseDataFactory


class {ModelName}Factory(BaseDataFactory):
    @classmethod
    def data(cls) -> list:
        return [
            {{"desc": "正常创建", "{field}": "{example}"}},
            {{"desc": "边界值", "{field}": "x" * 50}},
            {{"desc": "字段为空", "{field}": ""}},
        ]
```
